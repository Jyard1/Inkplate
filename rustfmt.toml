edition = "2021"
max_width = 100
use_small_heuristics = "Default"
newline_style = "Unix"
reorder_imports = true
reorder_modules = true

# imports_granularity = "Crate" and group_imports = "StdExternalCrate"
# would be nice but require nightly rustfmt — leave them out so the
# repo formats cleanly under the stable toolchain that CI uses.
